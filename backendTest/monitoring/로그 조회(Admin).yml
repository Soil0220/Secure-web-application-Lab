info:
  name: 로그 조회(Admin)
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/monitoring/admin
  headers:
    - name: Origin
      value: http://localhost:3000
    - name: X-Request-Id
      value: e16-e322-122d3-a2-446640000
    - name: X-Request-Time
      value: 2026-08-22T08:05:16.000Z
  body:
    type: json
    data: |-
      {
      "apiUrl" : "' ) UNION SELECT DISTINCT ROW_NUMBER() OVER(), name, null, null, null FROM users #"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
